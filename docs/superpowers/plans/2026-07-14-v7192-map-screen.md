# v7.19.2 Weekly Map Screen Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the Godot placeholder map with a content-driven, four-region, 64-city weekly map and city-detail interaction matching the Web Demo's single-player flow.

**Architecture:** Keep deterministic city data in `WeeklyMap`; let `main.gd` own presentation state (`current_region`, `state_popup`, offline clears) and input routing. Load the frozen v7.19.2 catalog once, draw the exact 16-node route for each region, and require an explicit detail-popup “进军” action before ruler selection.

**Tech Stack:** Godot 4.7, GDScript `Control` drawing/input, headless SceneTree interaction tests, viewport screenshot QA.

---

### Task 1: Specify the real map interaction

**Files:**
- Modify: `godot-demo/tests/test_main_flow.gd`

- [x] **Step 1: Add failing assertions**

After entering the map, assert that the catalog version is `7.19.2`, the weekly generator contains 64 cities, and the initial region is East. Click the first city and require `phase == "map"` plus `state_popup == 0`; click the popup go button and only then require `phase == "ruler"`. Also switch to the South tab and verify a locked city does not open.

- [x] **Step 2: Verify RED**

Run `test_main_flow.gd`; expect failure because the current scene has no catalog, weekly map, region tabs, or detail popup.

### Task 2: Bind v7.19.2 data and offline map state

**Files:**
- Modify: `godot-demo/src/app/main.gd`

- [x] **Step 1: Load the catalog and weekly generator**

Add:

```gdscript
const ContentCatalog = preload("res://src/content/content_catalog.gd")
const WeeklyMap = preload("res://src/progression/weekly_map.gd")
const CURRENT_WEEK := 2948

var catalog
var weekly
var current_region := 0
var state_popup := -1
var week_clears: Dictionary = {}
```

In `_ready()`, load `res://data/content-v7.19.2.json`, construct `WeeklyMap`, and fail loudly if content cannot load.

- [x] **Step 2: Add exact map geometry and unlock rules**

Use the Web Demo's 16 `STATE_POS` points and four `WEEK_TAB_RC` rectangles. Implement `_city_unlocked(k)` as `k == 0 or week_clears.has(k - 1)` for the initial offline slice.

### Task 3: Implement tabs, city details, and explicit march action

**Files:**
- Modify: `godot-demo/src/app/main.gd`
- Test: `godot-demo/tests/test_main_flow.gd`

- [x] **Step 1: Route map input**

When no popup is open, tabs change `current_region`; unlocked nodes open `state_popup`; locked nodes leave it closed. When a popup is open, `STATE_GO_RECT` calls `select_city(state_popup)`, while any other click closes the popup.

- [x] **Step 2: Draw content-driven map and detail panel**

For the active region, call `weekly.make_level(CURRENT_WEEK, k)` and draw real state name, chapter icon/color, foe triangle marker, locked/frontline state, and region theme. The detail panel must show enemy triangle/counter hint, HP multiplier, kill target, rules, terrain, first-clear gold, boss, and a “进军” button.

- [x] **Step 3: Verify GREEN**

Run `test_main_flow.gd` and expect `Godot main input and runtime flow: PASS`.

### Task 4: Visual and regression verification

**Files:**
- Create: `godot-demo/tests/capture_map_screen.gd`
- Generate (ignored): `output/godot/v7.19.2-week-map.png`

- [x] **Step 1: Capture the rendered map**

Instantiate `main.tscn`, advance to map, wait two frames, call `get_viewport().get_texture().get_image().save_png(...)`, and quit. Inspect the PNG for text clipping, node overlap, tab visibility, and popup readability.

- [x] **Step 2: Run complete regression**

Run all Godot headless tests, `npm test`, headless project import, and `git diff --check`.

- [x] **Step 3: Commit**

```text
feat: add v7.19.2 weekly map screen
```
