# v7.19.2 Opening Hero Pick Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the fixed three-hero placeholder with the Web v7.19.2 opening pick drawn from all 45 heroes, including foe-triangle bias, ruler-kin bias, and the one-non-DPS cap.

**Architecture:** Put the pure ranking/filtering rule in `OpeningPicker`, with an injected `RandomNumberGenerator` for reproducible tests. Keep `main.gd` responsible only for asking for choices after ruler selection and drawing the returned catalog heroes.

**Tech Stack:** Godot 4.7, GDScript, frozen v7.19.2 content catalog, headless tests.

---

### Task 1: Specify exact ranking and composition rules

**Files:**
- Create: `godot-demo/tests/test_opening_picker.gd`
- Create: `godot-demo/src/progression/opening_picker.gd`

- [x] **Step 1: Write the failing unit test**

Use equal base scores on a small hero set to prove `+0.15` for a hero that counters the foe, `-0.12` for a hero countered by the foe, `+0.18` for ruler kin, cosmetic-element exclusion, stable ordering, and at most one `shield`/`support`. Then load the real catalog and assert three unique IDs are returned from all 45 heroes.

- [x] **Step 2: Verify RED**

Run the test and expect failure because `opening_picker.gd` does not exist.

### Task 2: Implement the pure picker

**Files:**
- Create: `godot-demo/src/progression/opening_picker.gd`
- Test: `godot-demo/tests/test_opening_picker.gd`

- [x] **Step 1: Implement exact Web constants and bias**

Use `TRI_KE`, the frozen `ELEM_COSMETIC` IDs, universal `jiaxu` kin, and `lord_kin` catalog data. Rank by `randf() + bias`, preserve catalog order on ties, then scan until three heroes are chosen while allowing at most one `shield` or `support`.

- [x] **Step 2: Verify GREEN**

Run `test_opening_picker.gd`; expect `Godot v7.19.2 opening picker: PASS`.

### Task 3: Integrate the picker into the playable flow

**Files:**
- Modify: `godot-demo/src/app/main.gd`
- Modify: `godot-demo/tests/test_main_flow.gd`

- [x] **Step 1: Write failing flow assertions**

After selecting a ruler, assert that `opening_hero_ids` has three unique catalog IDs and contains no more than one non-DPS class. Select the first returned card and assert that exact ID is retained for battle.

- [x] **Step 2: Replace fixed hero IDs**

Construct `OpeningPicker` once, call it from `select_ruler()` using the selected city's foe triangle and `catalog.content.lord_kin`, and render/click `opening_hero_ids` instead of a constant list.

- [x] **Step 3: Verify flow and regressions**

Run all Godot tests, `npm test`, headless import, rendered capture, and `git diff --check`.

- [x] **Step 4: Commit and push**

```text
feat: add v7.19.2 opening hero draft
```
