# v7.19.2 Enemy Engagement and Ruler Combat Plan

**Goal:** Reproduce the frozen Web Demo's real front-line engagement, ruler auto attacks, and eight current signature commands in the Godot single-player run.

**Authority:** Frozen `reference/web-v7.19.2/index.html` sections `hurtUnit`, same-column blocker resolution, `LORDS`, `lordAutoOk`, `castLord`, and `LORD_ATK`.

**Architecture:** Keep orchestration in `battle_run.gd`, isolate ruler rules in a focused `battle_lord.gd`, and expose only render-ready state to `main.gd`. The run accepts a ruler meta level (default 1) so the later offline progression system can supply real levels without rewriting combat.

- [x] Add failing parity tests for same-column interception, 1.2/1.6-second enemy attack cadence, Web damage scaling, unit death/removal, and shield retaliation.
- [x] Implement ordinary enemy-versus-unit engagement and `hurt_unit` as the shared damage entry point, including taoyuan absorption hooks and formation recomputation on death.
- [x] Add failing tests for all eight `LORD_ATK` patterns, interval formulas, 3%-max-HP damage component, kin power, and lord-level scaling.
- [x] Implement ruler auto attacks for Cao Cao, Liu Bei, Sun Quan, Yuan Shao, Liu Biao, Gongsun Zan, Dong Zhuo, and Yuan Shu.
- [x] Add failing tests for the eight current auto conditions, cooldown rules, and level-1/2/3 signature values.
- [x] Implement 三才破敌、桃园义、截江断流、门生故吏、冰封、白马义从、火烧雒阳、僭号称帝 with their real battle consequences and deterministic test hooks.
- [x] Draw the wall ruler, command cooldown/readiness, active command effects, river, white-horse entity, and ruler attack traces in the Godot battle screen.
- [x] Add a capture scenario that visibly proves interception plus a ruler command, then run all Godot tests, editor import, rendered QA, code review, commit, and push.
